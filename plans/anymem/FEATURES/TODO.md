# anymem TODO Queue

## Next Up (P0)
1. `M-001` finalize canonical entities, workspace tenancy, scope, and package lineage records.
2. `M-009` lock standalone boundary and adapter contract model.
3. `M-002` define API contracts and service seams.
4. `M-010` finalize reusable approval object model.
5. `M-011` define retrieval ranking, tiered recall, and bounded deep expansion policy.
6. `M-004` define enforceable policy/procedure model and evaluation order.
7. `M-005` finalize append-only trace/proof and audit event shapes.
8. `M-003` finalize broker runtime lifecycle and concurrency/recovery controls.
9. `M-013` publish generic skill behavior contract.
10. `M-006` implement approval review surfaces with same-screen agent chat.

## After Core (P1)
1. `M-012` implement package export/install pipeline with sensitivity pruning and activation controls.
2. `M-007` implement dashboard memory/policy/approval/trace surfaces.
3. `M-008` implement mobile inbox flows.

## Rule
- anymem planning owns generic governance domains and does not depend on anycode implementation details.
